// Seed data is disabled to preserve user-created accounts and published properties.
const seedData = async () => {
    console.log('✅ Database ready. Real user data is preserved.');
};

module.exports = seedData;
